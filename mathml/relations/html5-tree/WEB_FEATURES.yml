features:
- name: clipboard-events
  files:
  - clipboard-event-handlers.tentative.html
# This following test validates semantics of the `dataset` feature, but it
# cannot be included in the classifier for that feature because it also covers
# unrelated functionality.
#- name: dataset
#  files:
#  - html-or-foreign-element-interfaces.tentative.html
