# GENERATED FILE DO NOT EDIT
# To regenerate this file run ./wpt make-tasks
allowPullRequests: collaborators
tasks:
- extra:
    github:
      branches: [master]
      events: [push]
  metadata: {description: '', name: wpt-chrome-dev-reftest-1, owner: '{{ event.head.user.email
      }}', source: '{{ event.head.repo.url }}'}
  payload:
    artifacts:
      public/results: {path: /home/test/artifacts, type: directory}
    command: [/bin/bash, --login, -c, ">- \n
        git clone {{event.head.repo.url}} \
          --single-branch
          --branch {{ event.head.repo.branch }} \
          --no-checkout \
          -q \
          --depth=50 \
          web-platform-tests \n
        cd web-platform-tests \n
        git checkout -b build {{event.head.sha}} \n
        cat wpt-report-11-of-15.json | python ./write-json.py /home/test/artifacts/rewritten.json"]
    image: gsnedders/web-platform-tests:0.13
    maxRunTime: 150
  provisionerId: aws-provisioner-v1
  workerType: '{{ taskcluster.docker.workerType }}'
version: 0
